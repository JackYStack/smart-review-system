# SmartReview 危大工程专项方案智能审查系统操作说明书

> 当前版本：2026-08-30 最终交付验收版  
> 适用对象：系统管理员、方案提交人员、复核专家、项目汇报人员  
> 系统定位：AI 辅助审查。AI 结果不能替代注册执业人员、项目技术负责人或专家的正式审查与签字。

## 1. 系统现在能够完成什么

系统目前已经跑通以下真实链路：

1. 用户登录并创建工程项目；
2. 选择已经发布的危大工程方案类型；
3. 上传 `.docx` 专项施工方案以及可选的辅助资料、现场图片；
4. LibreOffice 将 Word 转换为 PDF；
5. PaddleOCR PP-StructureV3 解析标题、正文、表格和公式；
6. 按模板执行结构、编制依据、跨章节一致性和章节内容审查；
7. 调用按方案类型配置的 Dify Workflow 进行全文审查；
8. 使用本地确定性规则和公式引擎进行必备章节、参数阈值与公式验算；
9. 在网页中逐条展示问题、原文证据、法规依据、定位和整改建议；
10. 生成 AI 批注版 Word 和独立的 Word 审查报告；
11. 将问题同步到专家复核任务池，支持领取、逐项处置、退回整改、批准和签发。

当前正式发布并可提交审核的方案类型只有：

- 脚手架工程 / 落地式钢管脚手架专项施工方案。

其他危大工程类型必须完成模板、法规、规则、公式、Dify Profile 和测试样本配置，并通过就绪检查与发布后，才会出现在提交页面。

## 2. 系统地址与推荐浏览器

- 部署电脑访问：<http://127.0.0.1/> 或 <http://localhost/>
- Tailscale/局域网地址：以一键部署脚本最后输出的地址为准；
- 完整范例任务：登录后在任务列表打开任务 12，或访问 <http://127.0.0.1/review/12/manual>。

推荐使用最新版 Edge 或 Chrome。其他电脑访问时必须满足以下条件之一：

- 与部署电脑处于同一局域网，并允许访问部署电脑的 80 端口；
- 两台电脑已经登录同一个 Tailscale 网络，并且访问控制规则允许互访。

如果打开地址后出现登录页，请使用管理员分配的账号登录。API 密钥、数据库密码和对象存储密码不得填写在普通业务页面中，也不要写入本说明书。

## 3. 用户角色

| 角色 | 主要权限 |
|---|---|
| 普通用户 | 创建项目、提交方案、查看本人任务、查看审查结果、下载报告 |
| 复核专家 | 查看复核任务池、领取任务、逐项确认或驳回问题、填写复核意见 |
| 复核管理员 | 管理复核任务、批准结果、签发正式报告 |
| 系统管理员 | 拥有全部业务权限，并可管理方案类型、模板、法规依据、规则、用户和系统设置 |

正式试点时，日常提交人员不应使用管理员账号。

## 4. 第一次启动系统

### 4.1 启动前检查

1. Docker Desktop 已启动；
2. Windows 虚拟化功能正常；
3. 仓库根目录存在 `.env.docker`；
4. `.env.docker` 中已经设置数据库、JWT、MinIO、管理员和外部服务配置；
5. Dify 或其他外部服务地址能够从部署电脑访问。

### 4.2 启动命令

在项目根目录打开 PowerShell，执行：

```powershell
docker compose --env-file .env.docker -f docker-compose.yml -f docker-compose.paddle.yml up -d --build
```

查看服务状态：

```powershell
docker compose --env-file .env.docker -f docker-compose.yml -f docker-compose.paddle.yml ps
```

正常情况下，以下服务均应显示 `running` 和 `healthy`：

- frontend；
- backend；
- worker；
- mysql；
- minio；
- onlyoffice；
- paddleocr。

首次启动 PaddleOCR 时需要准备模型，等待时间会明显长于其他服务。

停止系统：

```powershell
docker compose --env-file .env.docker -f docker-compose.yml -f docker-compose.paddle.yml down
```

## 5. 管理员上线前配置流程

应严格按照以下顺序配置。只创建一个方案类型名称并不能进行真实审核。

### 5.1 创建方案类型

进入“方案类型管理”，点击“新建方案类型”，填写方案大类、方案名称和备注。

方案类型具有以下生命周期：

- 草稿：仍在配置，不能提交审核；
- 待验证：已经提交发布验证；
- 已发布：就绪检查通过，可以提交审核；
- 已停用：不能创建新任务，但保留历史记录。

修改类型名称、模板、规则或分类型 Dify Profile 后，原发布状态会自动失效，必须重新提交验证并发布。

### 5.2 上传并配置 Word 模板

进入“模板管理”，找到对应方案类型并上传标准 `.docx` 模板。

系统会将模板转为 PDF，再由 PaddleOCR 提取章节树。解析完成后应人工检查：

- 标题层级是否正确；
- 正文是否归入正确章节；
- 表格是否被识别；
- 是否存在空标题、重复节点或遗漏章节；
- 章节审核提示词是否明确；
- 编制依据审核节点是否已经启用；
- 跨章节一致性对照关系是否正确。

### 5.3 配置编制依据

进入“编制依据管理”，为方案类型录入标准号、文件名称、效力状态、是否必引和备注。

正式使用前必须由专业人员核验：

- 标准是否现行；
- 是否适用于项目地区和工程类型；
- 条款号和条款原文是否准确；
- 是否存在地方标准或更严格规定。

### 5.4 配置规则与公式

进入“规则与公式”，先选择方案类型。

规则支持：

- 必备章节检查；
- 参数范围检查；
- 跨字段一致性检查；
- 公式验算。

每条正式规则都必须填写规范编号、条款号和条款原文。公式规则还必须配置变量映射、单位、表达式、比较符和阈值。

大模型可帮助提取候选参数，但最终数值判断由确定性代码执行。未经人工核验的模型参数只能形成“疑似问题，待人工复核”，不能直接作为正式违规结论。

“Excel 批量导入”页签提供规则与公式的批量数据准备说明。导入前应先预览校验结果并处理错误行。

### 5.5 配置 Dify Workflow

进入“设置 → 分类型Dify Profile”，为每种方案类型配置：

- 是否启用；
- API 地址；
- Workflow 应用密钥；
- 超时时间；
- 输出变量，当前约定为 `report`；
- 输出格式，正式发布应选择 JSON；
- 输入变量映射。

标准输入包括方案文件、现场图片、项目名称、项目地区、危大工程类型和审查重点。配置的目标变量名称必须与队友发布的 Dify Workflow 开始节点完全一致。

### 5.6 配置并测试知识库

进入“设置 → 知识库”，配置 Dify Dataset API 地址、Dataset API 密钥和可选的知识库名称前缀，然后点击“测试知识库连接”。

测试按钮会执行两层检查：

1. 能否读取知识库目录；
2. 能否真正检索一条知识片段。

“能看到知识库”不等于“知识库可以检索”。如果页面提示“目录可以访问，但实际检索失败”，应检查 Dify 的嵌入模型、索引状态、文档处理状态和 Dataset API 权限。

当前环境能够列出 3 个知识库，但真实检索测试失败。因此暂时不能把这些 Dataset 作为网站本地章节审查的可靠法规来源；Dify Workflow 内部的检索链路应单独检查。

### 5.7 提交验证并发布

返回“方案类型管理”：

1. 查看“就绪检查与缺失项”；
2. 所有缺失项处理完成后点击“提交验证”；
3. 状态变为“待验证”后点击“发布”；
4. 只有显示“已发布”的类型才能在方案审核页面选择。

## 6. 提交一个方案审核任务

### 6.1 准备文件

主方案必须为有效的 `.docx` 文件，建议直接使用系统模板编写。不要上传：

- 仅修改扩展名的假 Word 文件；
- 加密或损坏文件；
- 包含恶意宏或异常压缩内容的文件；
- 没有文字层且未经过处理的资料作为唯一主方案。

辅助资料支持 DOCX、PDF，最多 10 个；现场图片支持 JPG、PNG，最多 20 张。

### 6.2 创建项目并提交

进入“方案审核”：

1. 选择一个已经发布的方案类型；
2. 点击下载模板，可先按模板编制方案；
3. 点击提交审核；
4. 上传主方案 Word；
5. 选择已有项目，或者点击“新建并选择项目”；
6. 填写项目名称、项目所在地和参建单位；
7. 填写方案版本号，例如 `V1` 或 `V2-整改后`；
8. 可填写审查重点与补充说明；
9. 按需上传辅助文档和现场图片；
10. 点击“提交”。

重复点击或网络重试不会重复创建同一个任务。关闭提交窗口后重新发起，才会形成新的提交动作。

## 7. 查看任务状态和审查结果

任务列表区分四类状态：

### 7.1 技术处理状态

- 排队：尚未被 Worker 领取；
- 处理中：正在解析或审查；
- 成功：技术链路执行结束；
- 技术失败：系统或外部服务未能完成任务；
- 已取消：用户或管理员取消任务。

“技术成功”不等于“方案合规”。

### 7.2 审查结论

- 审查通过；
- 发现问题；
- 未形成结论。

### 7.3 结果完整性

- 完整：所有必需审查步骤均完成；
- 部分完成/降级：部分外部服务或证据链未完成；
- 不可用：无法形成可依赖的审查结果。

### 7.4 人工复核状态

- 待人工复核；
- 复核中；
- 待整改；
- 已批准；
- 已签发。

点击“人工审阅”进入结果详情。页面按照以下中文步骤展示：

1. 结构审核；
2. 编制依据审核；
3. 上下文一致性；
4. 内容审核；
5. Dify Workflow 全文审查；
6. 规则与公式验算。

每条问题应尽量包含问题描述、方案原文、章节或段落定位、法规依据、证据状态和整改建议。

证据状态含义：

- 已核验：证据链满足当前系统判定条件；
- 尚未核验：缺少规范编号、条款号、条款原文或可定位方案证据；
- 疑似问题，待人工复核：AI 发现了风险，但不能直接认定为正式违规；
- 需要专业人员判断：必须由专家结合工程实际确认。

## 8. 下载和编辑 Word 成果

任务完成后可下载：

- 原始方案；
- AI 批注版方案；
- 独立 Word 审查报告；
- 专家修改版；
- 正式签发报告。

只有实际生成的成果才会显示下载入口。技术失败时不得把原始文件冒充为审核成果。

点击预览或编辑时，系统通过 OnlyOffice 打开文档。AI 批注版中的批注数量可能少于网页问题总数，因为只有能够定位到具体段落的问题才能写入 Word 批注；未精确定位的问题仍会保留在网页问题清单和独立审查报告中。

## 9. 专家复核操作

专家或管理员进入“专家复核”：

1. 在任务池找到“待领取”任务；
2. 点击“领取”；
3. 打开复核详情，阅读 AI 问题、原文证据和法规依据；
4. 对每个问题选择接受、驳回或修改等级，并填写意见；
5. 如方案需要修改，选择“退回整改”；
6. 提交人上传整改版本后进行复审；
7. 全部问题处理完成后，由有权限的人员批准；
8. 复核管理员执行签发，生成带签发信息和哈希的正式报告。

已签发文件禁止覆盖。原始版、AI 版、专家版和签发版分别保存，以便追溯。

## 10. 当前演示范例

仓库 `demo-data` 目录已经提供一个跑通的脚手架范例：

- `脚手架专项施工方案_缺陷演示样本.docx`；
- `任务12_AI批注方案.docx`；
- `任务12_智能审查报告.docx`。

任务 12 的实际结果：

- 技术状态：成功；
- 结果完整性：完整；
- 审查结论：发现问题；
- 问题总数：26；
- Dify 结构化问题：4；
- 规则与公式：2 项确定性校验，其中公式通过 1 项、必备章节规则发现问题 1 项；
- 专家状态：待领取。

该范例中的规范关联和规则参数均明确标记为演示配置。公式输入由管理员以带来源、单位、验证人和时间的方式录入，计算结果为 `12 kN / 15 kN = 0.8 ≤ 1`；不得直接用于真实施工审查。

## 11. 常见问题处理

### 11.1 网页无法访问

先确认 Docker Desktop 正常，再执行 `docker compose ... ps`。如果前端不是 healthy，应查看前端和后端日志。其他电脑访问时还要检查防火墙、Tailscale 连接和访问地址。

### 11.2 登录失败

确认输入的是系统用户账号，不是 Dify 账号。管理员可以在“用户管理”中检查账号角色和状态。不要通过修改数据库的方式随意重置正式环境密码。

### 11.3 方案类型没有选项

只有同时满足“模板已配置、规则与公式已配置、外部服务可用、就绪检查通过、已经发布”的方案类型才会出现。

### 11.4 任务长时间处理中

查看任务日志，判断停留在 PaddleOCR、模型、Dify Workflow 还是规则引擎。大文档和 CPU 版 PaddleOCR 可能耗时较长；不要在处理中重复提交大量相同任务。

### 11.5 Dify 显示 succeeded，但网站显示部分完成

常见原因是 Dify 输出虽然存在，但没有完全符合网站 JSON Schema，例如字段类型错误、缺少 `report`、问题缺少证据或建议。应统一 Workflow 最终输出格式，并检查网站保存的 Workflow 运行 ID。

### 11.6 知识库目录成功，但检索失败

说明网络和密钥能够读取目录，但知识库本身不能完成向量检索。重点检查嵌入模型、索引、文档状态和权限，而不是继续修改网站地址。

### 11.7 Word 批注少于网页问题

这是定位精度差异。能够定位到段落的问题写入 Word 批注；其余问题保留在网页和审查报告中。应继续完善 Paddle 页码、段落坐标和表格单元格定位。

## 12. 正式使用前必须完成的工作

1. 由专业人员核验法规条款、适用条件、规则阈值和公式；
2. 为计划上线的每种危大工程分别完成模板、规则、公式、Dify 和缺陷样本；
3. 完成专家标注测试集和准确率评估；
4. 轮换聊天记录中公开过的管理员、Dify 和存储凭据；
5. 使用 HTTPS 单入口，隐藏 MySQL、MinIO、PaddleOCR 等内部端口；
6. 建立数据库和文档备份、恢复演练、监控告警和日志保留；
7. 完成正式用户、专家和管理员的最小权限划分；
8. 明确系统仅提供辅助审查，最终责任由有权限的人工人员承担。

## 13. 相关资料

- 项目总体说明：`README.md`；
- 新手启动说明：`docs/新手启动说明.md`；
- 部署与恢复：`docs/DEPLOYMENT_AND_RECOVERY.md`；
- Paddle 解析说明：`docs/PADDLE_PARSER.md`；
- 当前实现状态：`docs/IMPLEMENTATION_STATUS_20260825.md`。
