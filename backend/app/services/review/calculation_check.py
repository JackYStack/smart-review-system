from app.core.issue_card import IssueCard
from app.core.review_context import ReviewContext


async def check_calculations(context: ReviewContext) -> list[IssueCard]:
    """Check high-risk calculation templates."""

    return []
