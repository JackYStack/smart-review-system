from app.core.issue_card import IssueCard
from app.core.review_context import ReviewContext


async def check_procedure(context: ReviewContext) -> list[IssueCard]:
    """Check procedural compliance."""

    return []
